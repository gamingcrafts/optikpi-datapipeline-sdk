# Optikpi Data Pipeline SDK Configuration
# Copy this file to 'env' and fill in your actual values

# Required: Your Optikpi authentication token
OPTIKPI_AUTH_TOKEN=your_auth_token_here

# Required: Your Optikpi account ID
OPTIKPI_ACCOUNT_ID=your_account_id_here

# Required: Your Optikpi workspace ID
OPTIKPI_WORKSPACE_ID=your_workspace_id_here

# Optional: Base URL for the API (defaults to demo environment)
OPTIKPI_BASE_URL=https://demo.optikpi.com/apigw/ingest

# Optional: Timeout in milliseconds (default: 30000)
OPTIKPI_TIMEOUT=30000

# Optional: Number of retries (default: 3)
OPTIKPI_RETRIES=3

# Optional: Retry delay in milliseconds (default: 1000)
OPTIKPI_RETRY_DELAY=1000
